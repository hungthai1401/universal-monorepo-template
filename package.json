{
  "name": "universal-monorepo-template",
  "version": "1.0.0",
  "description": "Monorepo project with VueJs and Laravel",
  "main": "index.js",
  "author": "hungthai1401",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
	"bootstrap": "lerna bootstrap --use-workspaces",
    "start": "lerna run start --parallel",
    "build": "lerna run build --parallel"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
